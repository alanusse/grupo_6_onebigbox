<!DOCTYPE html>
<html lang="es">
<!-- Include Head -->
<%- include('../partials/head', {title: 'Listado de Recetas'}) %>
<body>
    <!-- Header -->
	<%- include('../partials/header-admin') %>
    <!-- /Header -->
    
    <div class="list-header">
        <h1>Recetas</h1>
        <a href="./recetas/abm-recetas-alta" class="recipe-btn recipe-create"><i class="fas fa-plus"></i>Crear receta</a>
    </div>
    <main class="recipes-list">
        <% recetas.forEach(receta => { %>
            <div class="recipe-item">
                <div class="recipe-info">
                    <img src="/img/recetas/<%= receta.image %>" alt="recipe_image">
                    <h2><%= receta.titulo %></h2>
                </div>
                <div class="recipe-actions">
                    <a href="/admin/recetas/abm-recetas-modificacion/<%= receta.id %>" class="recipe-btn recipe-modify"><i class="fas fa-edit"></i>Modificar</a>
                    <form action="/admin/recetas/eliminar-receta/<%= receta.id %>" method="POST" class="recipe-btn recipe-delete btn-eliminar">
                        <i class="fas fa-trash-alt"></i>
                        <input type="submit" value="Eliminar">
                    </form>
                </div>
            </div>
        <% }) %>
    </main>
    <script src="/js/script.js"></script>
</body>
</html>