<!DOCTYPE html>
<html lang="es">
<%- include('../partials/head', {title: 'Alta Plan'}) %>
<body>
	<%- include('../partials/header-admin') %>

    <form action="../abm-planes-modificacion/<%= plan.id %>" method="POST" class="recipe-update" enctype="multipart/form-data">
        <h1>Modificar Plan</h1>
        
        <% if (typeof errors != 'undefined') { %>
            <span>
                <ul style="background-color: #ff6868; padding: 15px 20px; border-radius: 10px; width: 100%">
                    <% errors.forEach(error => { %>
                        <li style="color: #fff; list-style: disc; margin-left: 20px; font-size: 14px;"><%= error.msg %></li>
                    <% }) %>
                </ul>
            </span>
        <% } %>
        
        <span>
            <i class="fas fa-box-open"></i>
            <input type="text" name="plan" id="plan" placeholder="Nombre del Plan" value="<%= plan.plan %>">
        </span>

        <span>
            <i class="fas fa-sticky-note"></i>
            <input type="text" name="description" id="description" placeholder="Descripción" value="<%= plan.description %>">
        </span>
        
        <label for="recipe-img" class="recipe-img">
            <i class="fas fa-upload"></i>Cargar imagen
            <input type="file" name="image" id="image" class="recipe-img" value="/img/planes/<%= plan.image %>">
        </label>

        <div class="update-action-btns">
            <a href="/admin/planes">Cancelar</a>
            <input type="submit" value="Modificar Plan">
        </div>
    </form>
</body>
</html>